---
# Remove default OpenShift security-notice banner
# ArgoCD will delete this resource from the cluster during sync
apiVersion: console.openshift.io/v1
kind: ConsoleNotification
metadata:
  name: security-notice
  annotations:
    argocd.argoproj.io/sync-options: Delete=true
spec:
  text: ""
