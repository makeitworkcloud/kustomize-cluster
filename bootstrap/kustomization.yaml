---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Wave 0: Configure ArgoCD (KSOPS + GitHub OAuth + RBAC)
  - argocd-config.yaml
  - wait-for-repo-server.yaml
  # Wave 1: Deploy workloads after repo-server is ready
  - workloads-app.yaml
