---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Operator from upstream GitHub release
  - https://github.com/adyanth/cloudflare-operator.git/config/default?ref=v0.13.1
  # ClusterTunnel CR (cloudflared deployment created by operator)
  - cluster-tunnel.yaml
generators:
  - ksops-cloudflare-secrets.yaml
patches:
  # Remove resource limits from operator deployment (keep requests only)
  - patch: |
      - op: remove
        path: /spec/template/spec/containers/0/resources/limits
    target:
      kind: Deployment
      name: cloudflare-operator-controller-manager
      namespace: cloudflare-operator-system
