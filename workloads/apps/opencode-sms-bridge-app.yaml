apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: opencode-sms-bridge
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  sources:
    - chart: opencode-sms-bridge
      repoURL: ghcr.io/makeitworkcloud/charts
      targetRevision: 0.1.3
      helm:
        values: |
          image:
            tag: "9ae1ead0f55c7040cdc7bdd3611718bc6b56cf62"
          config:
            canonicalWebhookUrl: https://sms.makeitwork.cloud/twilio/inbound
            messagingServiceSid: "MG6d0d09f5cc56feb808d435c8a5aade13"
    - repoURL: https://github.com/makeitworkcloud/kustomize-cluster.git
      path: workloads/opencode-sms-bridge
      targetRevision: main
  destination:
    server: https://kubernetes.default.svc
    namespace: opencode
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
    retry:
      limit: 5
      backoff:
        duration: 30s
        maxDuration: 5m
        factor: 2
