---
# TEMPLATE ONLY — replace the empty value locally with the dedicated SSH private
# key, then encrypt this file with SOPS before pushing. Never commit the PEM in
# plaintext. The path-specific .sops.yaml rule encrypts only privateKey.
apiVersion: v1
kind: Secret
metadata:
  name: mcp-hero-ssh
  namespace: mcp
  annotations:
    argocd.argoproj.io/sync-wave: "1"
type: Opaque
stringData:
  privateKey: ""
