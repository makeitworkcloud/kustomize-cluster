# Replace the empty value with `Bearer <your SlideSpeak API key>` and encrypt
# the `authorization` field with SOPS before this draft PR is made ready.
apiVersion: v1
kind: Secret
metadata:
  name: mcp-slidespeak
  namespace: mcp
type: Opaque
stringData:
  authorization: ""
