---
apiVersion: v1
kind: Service
metadata:
  name: opencode-memory-pilot
  namespace: opencode
  annotations:
    argocd.argoproj.io/sync-wave: "1"
    ignore-check.kube-linter.io/dangling-service: "Targets the opencode-memory-pilot Deployment rendered by the chart source of this Application"
spec:
  selector:
    app: opencode-memory-pilot
  ports:
    - name: http
      port: 4096
      targetPort: http
