---
apiVersion: v1
kind: Service
metadata:
  name: opencode-sms-bridge
  namespace: opencode
  annotations:
    argocd.argoproj.io/sync-wave: "1"
    ignore-check.kube-linter.io/dangling-service: "Targets the opencode-sms-bridge Deployment rendered by the chart source of this Application"
spec:
  selector:
    app: opencode-sms-bridge
  ports:
    - name: http
      port: 80
      targetPort: http
